# 项目改进 ToDo — 2026-04-20

本文档是 `2026-04-20` 的执行快照，不替代长期状态板。长期状态仍以 `docs/project_progress.md` 为准。

分类标记：
- `🔴` = 明天应该直接执行，服务主线
- `🟡` = 可并行推进，但不能抢主线训练窗口
- `🔵` = 观察/收尾项

配套参考：
- 主线方案：`docs/xmodel1/xmodel1_model_design_v1.md`
- 备线方案：`docs/keqingv4/keqingv4_model_design_v2.md`
- 状态板：`docs/project_progress.md`
- 协作入口：`docs/agent_sync.md`

---

## 0. 执行边界
- 用户负责实际执行 `preprocess / train / review / 强度验收`
- agent 负责代码对齐、Rust 语义 ownership、contract/gate/harness 收口
- 旧运行面已经归档，明天不再回头修旧模型逻辑，也不重新激活旧 deploy/旧 cache

## 1. xmodel1 主线

### X1. 跑正式 full preprocess gate 🔴
- 执行 `ds1 + ds2 + ds3` 全量 preprocess gate
- 确认 manifest、required shard、sample probe 全绿
- 抽样确认：
  - `candidate_feat`
  - `special_candidate_feat`
  - `event_history`
  - `opp_tenpai`
  - `pts_given_*`

通过标准：
- `processed_xmodel1/*` 三个数据根齐全
- manifest 与实际 shard 一致
- gate 不再暴露字段缺失或 shape 异常

### X2. 跑 smoke train 🔴
- 用现有 focused train-time gate 先挡一遍 loader/manifest/batch 合同
- 再做最小 smoke train，不扩结构，不改 schema

通过标准：
- 训练入口能稳定加载 `ds1 + ds2 + ds3`
- batch 中关键字段非空且 shape 正常
- 不出现新的训练前 contract 断裂

### X3. 跑 slice_report + review 🔴
- 统一使用现有 slice harness
- 重点看：
  - `reach`
  - `call`
  - `none`
  - `hora`
  - `chi_low / chi_mid / chi_high`

通过标准：
- 有一套明确的 slice 结果
- review/replay 能支持是否冻结当前 baseline 的判断

### X4. 只收 correctness，不扩结构 🟡
- 如果 full preprocess / smoke train 后仍暴露 `candidate_feat` 或 `state_tile_feat` drift
- 继续把问题收口到明确 case / slot / owner 边界
- 不新增 head，不改 `xmodel1_discard_v2` 合同

## 2. keqingv4 备线

### K1. 保持备线，不进训练窗口 🟡
- 明天默认不执行 `keqingv4` 训练
- 只有 `xmodel1` 触发 kill criteria，才切到 `keqingv4` 的 preprocess/train/eval

### K2. 继续 Phase B 代码侧收口 🟡
- 继续做非递归 truth strengthening
- 继续把 continuation compare / aggregation 压到 shared Rust pure helpers
- 继续减少 Python-owned summary/projection 残留

目标：
- 保持 `keqingv4` 的 train-ready 状态
- 不占用主线窗口

## 3. 共享 Rust 与清理收尾

### C1. 缩小 scoring fallback 面 🟡
- 继续检查 `mahjong_env/scoring.py` 是否还存在不必要 Python fallback
- focused regression 继续围绕：
  - fallback 只在 capability 缺失时允许
  - 正常 native path 不应静默落回 Python

### C2. 观察 `processed_v3.retired` 🔵
- 明天不再允许任何活跃脚本、配置、文档重新引用它
- 如果全天无新消费者出现，下一轮直接永久删除

### C3. 保持 archive-only 政策 🔵
- 历史 runtime 文档与旧 deploy 模板只允许留在 archive
- 不允许把旧 service 名、旧 bot alias、旧 checkpoint 名重新带回主面

## 4. 验收顺序
1. 先跑 `xmodel1` full preprocess gate
2. gate 通过后跑 smoke train
3. 再跑 `slice_report + review`
4. 最后决定是否冻结当前 baseline

## 一句话总结
`2026-04-20` 的重点不是再开新设计，而是把 `xmodel1` 主线真正走完一轮 `full preprocess -> smoke train -> slice/review`，同时让 `keqingv4` 继续维持备线 train-ready，不抢主线窗口。
