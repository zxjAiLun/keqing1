# 项目改进 ToDo — 2026-04-20

本文档是 `2026-04-20` 的执行快照，不替代长期状态板。长期状态仍以 `docs/project_progress.md` 为准。

## 今日摘要

今天项目的正确读法是：
- `xmodel1` 主线已经完成当前 code-side 合同收口，重点不再是继续改 schema 或继续加结构。
- 真正待补的是主线证据链：`full preprocess -> smoke train -> slice/review acceptance`。
- `keqingv4` 继续做备线冻结与 Rust ownership 边界验证，不默认进入训练窗口。
- 共享 Rust 继续推进，但只能在不打断主线证据链的前提下做 fail-closed 边界巩固。

如果今天只记住一件事，应记住：
- 当前全局目标不是“再设计一版”，而是把已经冻结的主线真正跑完一轮可审查的训练前闭环。

分类标记：
- `🔴` = 今天应直接执行，服务主线
- `🟡` = 可并行推进，但不能抢主线训练窗口
- `🔵` = 观察/收尾项

配套参考：
- 主线方案：`docs/xmodel1/xmodel1_model_design_v1.md`
- 备线方案：`docs/keqingv4/keqingv4_model_design_v2.md`
- 状态板：`docs/project_progress.md`
- 协作入口：`docs/agent_sync.md`
- Rust 计划：`plans/rust_ownership_push_2026_04_19.md`

---

## 0. 今日代码事实
- `xmodel1` 主线今天已经从“继续跑 `xmodel1_discard_v2`”切到“`xmodel1_discard_v3` schema cutover 已冻结并过门禁”。当前 code-side v3 合同与小样本 profile gate 已绿，full preprocess 可以在现行 v3 合同上继续。
- `xmodel1` Rust preprocess 仍保留决策级 discard candidate 复用与 `XMODEL1_EXPORT_PROFILE=1` 分段计时，但公开 contract 已改为压缩版：删 raw `event_history`，改 `history_summary`，并同步压缩 candidate/special slot。
- `xmodel1` Python loader/runtime/train 面已经完成一轮 v3 收口：parity oracle 不再导出 `event_history`，shared trainer 已接管训练主循环，checkpoint metadata 与 legacy runtime shape inference 也已统一。
- native Rust `build_xmodel1_runtime_tensors(...)` 已落地进扩展实现，并补了“decision snapshot runtime 重算 vs v3 导出行”一致性测试；Python 只在 missing capability 时回退。
- shared scorer 的 continuation fallback 已补上 batched `forward_many(...)` 路径；当前 beam 行为不变，但 forward 次数下降。
- `keqingv4` 备线今天继续推进 Rust ownership 收口：future-truth、continuation scenario/scoring/aggregation、hora truth、public legal actions、runtime replay snapshot 都更明确地变成 Rust-first，Python 仅保留 missing-capability emergency path。
- 共享语义面继续去 legacy：`mahjong_env.action_space`、`mahjong_env.feature_tracker`、`mahjong_env.progress_oracle`、`training.state_features` 已成为现行共享入口；旧 `keqingv1/v2/v3/v31` 包及其专属脚本/配置已从 active tree 删除。
- 今天剩余重点已经从“先补 gate”切到“直接推进 full preprocess -> smoke train -> slice/review 证据闭环”，同时保持 cooperative Ctrl-C/resume 与 profile spot check 可回归。

## 1. xmodel1 主线

### X1. 冻结 v3 合同边界 🔴
- 明确停掉所有 `xmodel1_discard_v2` full preprocess 计划。
- 把 exporter / loader / runtime / trainer / review 的公共字段统一切到 `xmodel1_discard_v3`。
- 已完成：
  - `history_summary` shared owner 对齐
  - v3 loader / checkpoint fail-close
  - shared trainer 接管 xmodel1 epoch loop
  - legacy xmodel1 checkpoint 的 runtime 自动识别回归修复
- 仍待完成：
  - 小样本 profile / Ctrl-C / 文件级 resume 证据补齐

通过标准：
- 文档、代码、测试都不再宣称“继续跑 v2 full preprocess”。
- `xmodel1` 仍是 mainline，`keqingv4` 仍是 backup。
- v3 cache/runtime/checkpoint 的 fail-close 行为明确。

### X2. 跑小样本 profile gate（已通过）🔵
- 固定使用 `XMODEL1_EXPORT_PROFILE=1`。
- 样本只取 `ds1 + ds2 + ds3` 的固定小子集；不要直接开全量。
- 验证：
  - `discard_candidate_analysis + special_summary` wall time 至少下降目标线
  - `Ctrl-C` 中断后可安全退出
  - 第二次同命令可 resume
  - 所有 v3 contract tests 通过

当前证据：
- 固定 `ds1 + ds2 + ds3` 15 文件 / 8843 samples 复测已到 `total_wall=1.386s`。
- 同子集 `discard_candidate_analysis` 已从旧 profile 的 `545.964s` 累计阶段降到 `1.891s`。

### X3. 开 full preprocess / smoke train / slice review 🔴
- 顺序固定：
  1. full preprocess
  2. smoke train
  3. slice_report + review
- 重点切片仍是：
  - `reach`
  - `call`
  - `none`
  - `hora`
  - `chi_low / chi_mid / chi_high`

## 2. keqingv4 备线

### K1. 保持备线，不进训练窗口 🟡
- 今天默认不执行 `keqingv4` 训练。
- 只有 `xmodel1` 触发 kill criteria，才切到 `keqingv4` 的 preprocess/train/eval。

### K2. 做完 Phase B focused verification 🔴
- 不再扩 future-truth 深度，也不继续做结构加法。
- 只验证今天已经落地的 Rust-first 路径：
  - `tests/test_keqingv4_preprocess.py`
  - `tests/test_keqingv4_inference_adapter.py`
  - `tests/test_model_v4_shapes.py`
  - `tests/test_training_v4_smoke.py`
  - `tests/test_inference_contracts.py`
  - `tests/test_scoring_backend_boundary.py`
  - `scripts/evaluate_keqingv4_special_calibration.py`
  - `scripts/evaluate_keqingv4_special_calibration_from_replays.py`

通过标准：
- continuation / future-truth / hora truth / public legal actions / replay snapshot 的 Rust-first 路径全部保持绿灯。
- fallback 只在 missing capability 时触发，unexpected drift 必须直接报错。
- 验证通过后，把当前 `keqingv4` 视为 frozen backup snapshot。

## 3. 共享 Rust 与边界冻结

### C0. 按计划只开第一拍 🟡
- 执行计划文件：`plans/rust_ownership_push_2026_04_19.md`
- 今天只做已冻结范围，不提前进入 `xmodel1` runtime feature Rust 化。
- 默认顺序：
  1. `S0` boundary freeze and guardrails
  2. `S1` keqingv4 fail-closed cleanup
  3. `S2` legal action public-owner consolidation

### C1. 核对 public legal / replay snapshot / hora truth 边界 🔴
- 重点检查三条公开语义面：
  - `mahjong_env.legal_actions`
  - `inference.default_context`
  - `mahjong_env.scoring`
- 目标是确认“Rust public owner + Python missing-capability fallback only”已成立，而不是继续容忍 drift-tolerant 混合 ownership。

通过标准：
- `legal_actions` 不再混用“Rust structural + Python public assembly”作为默认路径。
- runtime snapshot 默认取 Rust replay snapshot。
- unexpected native drift 不会静默落回 Python scoring。

### C2. 共享中性模块继续维持冻结 🟡
- 不允许重新引入：
  - `keqingv1.action_space`
  - `keqingv3.feature_tracker`
  - `keqingv3.progress_oracle`
  - `keqingv3.features`
- 统一继续使用：
  - `mahjong_env.action_space`
  - `mahjong_env.feature_tracker`
  - `mahjong_env.progress_oracle`
  - `training.state_features`

## 4. 观察与收尾

### O1. 继续观察 `processed_v3.retired` 🔵
- 今天不允许任何活跃脚本、配置、文档重新指向它。
- 若全天无消费者出现，下一轮清理直接永久删除。

### O2. 保持 archive-only 政策 🔵
- 旧 runtime 文档、旧 deploy 模板、旧模型族脚本/配置只允许留在 archive 或历史提交中。
- 不允许把旧 bot alias、旧 service 名、旧默认训练入口重新带回主面。

## 5. 今日后续验收顺序
1. 先跑 `xmodel1` full preprocess gate。
2. gate 通过后跑 `xmodel1` smoke train。
3. 再跑 `slice_report + review`，决定是否冻结当前 baseline。
4. 并行完成 `keqingv4` Phase B focused verification，但不打开训练窗口。

## 一句话总结
`2026-04-20` 的任务不是再加结构，而是把 `xmodel1` 主线真正走完一轮 `full preprocess -> smoke train -> slice/review` 证据闭环，同时把 `keqingv4` 与共享 Rust 公共语义面的 fail-closed 边界验证到位，冻结成可继续推进的备线与共享核心快照。
