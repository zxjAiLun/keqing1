# Xmodel1 预处理配置（Rust full export）
data_dirs:
  - artifacts/converted_mjai/ds1
  - artifacts/converted_mjai/ds2
  - artifacts/converted_mjai/ds3

output_dir: processed_xmodel1_v1b
schema_name: xmodel1_discard_v2
schema_version: 2
progress_every: 20
jobs: 0  # 0 = auto (use available CPU cores)
preflight_files_per_shard: 2
preflight_seed: 20260419

# Rust full exporter 已经成为唯一预处理路径，以下维度用于协议校验。
max_candidates: 14
candidate_feature_dim: 35
candidate_flag_dim: 10
special_candidate_feature_dim: 25
max_special_candidates: 12
