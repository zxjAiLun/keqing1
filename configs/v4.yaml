# ModelV4 训练配置

# 模型参数
tile_plane_dim: 32
scalar_dim: 24
hidden_dim: 256
num_res_blocks: 3
dropout: 0.1

# 训练参数
learning_rate: 0.001
weight_decay: 0.0001
batch_size: 256
num_epochs: 20
grad_clip: 1.0
train_split: 0.9

# 模型名称 (用于输出目录)
model_name: "modelv4"
